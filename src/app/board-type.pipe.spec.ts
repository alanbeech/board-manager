import { BoardTypePipe } from './board-type.pipe';

describe('BoardTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BoardTypePipe();
    expect(pipe).toBeTruthy();
  });
});
